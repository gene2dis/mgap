config {
    testsDir "tests"
    workDir ".nf-test"
    configFile "nextflow.config"
    profile "docker"

    plugins {
        load "nf-schema@2.2.0"
    }

    stage {
        copy "assets"
        copy "conf"
        copy "modules"
        copy "subworkflows"
        copy "workflows"
    }
}
