config {
    testsDir "tests"
    workDir ".nf-test"
    configFile "nextflow.config"
    profile "docker"

    stage {
        copy "assets"
        copy "conf"
        copy "modules"
        copy "subworkflows"
        copy "workflows"
    }
}
